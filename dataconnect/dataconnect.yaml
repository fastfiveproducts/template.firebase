specVersion: "v1"
serviceId: "template-9960d-service"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "template-9960d-database"
      cloudSql:
        instanceId: "template-9960d-instance"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
